import Foundation

class SwipeManager {
    static let shared = SwipeManager()
    private init() {}
    
    func swipe(user: User, on otherUser: User) -> Bool {
        // Add logic to record swipe and check for mutual swipe
        // Return true if mutual swipe
        return false
    }
}
